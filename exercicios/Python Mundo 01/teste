lista = list()

for _ in range(0,5):
    valor= int(input('Digite um numero: '))
    lista.append(valor)
    
maiorNumero=max(lista)
menorNumero=min(lista)

if valor == maiorNumero:
    for i in range(0, len(lista)):
        pass
print(f'posição {i} maior {maiorNumero}')
for i in range (0, len(lista)):
    
print(f'posição menor {menorNumero}')