import random
import string
jogos= list()

def geraradorAleatorio():   
    seisNumeros=''.join(random.choices(string.digits, k=6))

    while seisNumeros in jogos:
        seisNumeros=''.join(random.choices(string.digits, k=6))
    jogos.append(seisNumeros)
    return seisNumeros

print(jogos)